---
aliases:
  - "`CircularArray`#`addLast(e)`"
---

# `addLast(e)`

```java
@Override
public void addLast(E e) {
    Objects.requireNonNull(e);

    // tail：next最后一个用户元素的索引
    elements[tail] = e;
    tail = (tail + 1) & (elements.length - 1);

    if (head == tail) {
        doubleCapacity();
    }
}
```

- [`doubleCapacity()`](05-002-02-06-02-06.java.md)
