# 常用命令-压缩解压缩类 16 视频day02 Linux文献

- 压缩的好处：1）节省存储空间；2）节省I/O。
- `tar -czvf TAR_NAME.tar.gz PATH...`：打包并压缩。
    - `-c`：打包。
    - `-z`：`gzip`压缩或`gunzip`解压缩。
    - `-v`：打印信息。
    - `-f`：包名。
- `tar -zxvf TAR_NAME.tar.gz -C DIR`：解压缩并解包。
    - `-x`：解包。
    - `-C`：解到指定目录。
