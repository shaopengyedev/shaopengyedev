# 函数 11-12 视频day01 Shell文献

- 系统函数：
    - `basename PATH [SUFFIX]`：提取文件名。
        - `PATH`：绝对路径或相对路径。
            - 路径可以不存在。
            - 路径末尾可以有`/`。
        - `SUFFIX`：需要去除的后缀。
    - `dirname PATH`：提取父目录的路径（末尾没有`/`）。
        - `PATH`：同`basename`的`PATH`。
